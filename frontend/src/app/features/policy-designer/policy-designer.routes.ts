import { Routes } from '@angular/router';
import { PolicyDesignerComponent } from './policy-designer.component';

export const POLICY_DESIGNER_ROUTES: Routes = [
  { path: '', component: PolicyDesignerComponent },
];
